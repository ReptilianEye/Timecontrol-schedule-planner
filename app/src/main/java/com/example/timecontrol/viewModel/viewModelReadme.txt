deleting existing Instructor or other entity class object by id
    viewModel.deleteInstructor(viewModel.getInstructorById(2).instructor)

deleting all Instructors or other entity class objects
    viewModel.deleteAllInstructors()

inserting new Instructor or other entity class object
    viewModel.insertInstructor(instructor)

updating existing Instructor or other entity class object
    viewModel.updateInstructor(instructor)